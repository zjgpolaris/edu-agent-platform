# Compatibility entry point. Runtime and evaluation dependencies are split so
# production installs do not need the heavier evaluation stack.
-r requirements-runtime.txt
-r ../eval/requirements-eval.txt
