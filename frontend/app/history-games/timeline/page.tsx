import TimelineGameClient from "./TimelineGameClient";

export const metadata = {
  title: "时间线修复师 | EduAgent",
  description: "通过排序闯关复原历史事件时间线。",
};

export default function TimelineGamePage() {
  return <TimelineGameClient />;
}
